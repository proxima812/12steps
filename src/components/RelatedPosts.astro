---
import PostCard from "./PostCard.astro";
const { posts } = Astro.props;
---

<h2 class="text-3xl font-semibold underline underline-offset-4 text-gray-800">🎩 Похожие посты</h2>
<div class="mb-12 grid gap-4 md:grid-cols-2">
  {
    posts
      .slice(0, 3)
      .map((post) => <PostCard data={post.data} slug={post.slug} />)
  }
</div>
