---


const { date, ...attrs } = Astro.props;
---

<time datetime={date.toISOString()} {...attrs}>
  🗓 {
    date.toLocaleDateString("ru-RU", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
